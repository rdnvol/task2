{% layout none %}
{
    "products": [
    {% paginate collection.products by settings.collection_limit %}
        {%- for product in collection.products -%}
            {% render 'product-json', product: product %}{% unless forloop.last %},{% endunless %}
        {%- endfor -%}
    {% endpaginate %}
    ]
}